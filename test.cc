#include <iostream>

using namespace std;

int main()
{
	if(true)
		;
	else if(true){
	}
	//if(true);
	/*if (true)
	 *a=b;
	 if(true)*/
	if/* if */ (true)
		;
	if(true)
		if(true)
			;
	if(true)
		for(;;);
	string str='//
		//';if(true);
}
